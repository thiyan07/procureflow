# ProcureFlow Feature Audit — Full Project Inspection (2026-09-18)

**Scope:** Flutter `lib/` 14 screens + FastAPI `backend/app` 13 routers 18 tables + PostgreSQL/SQLite + FCM mock + `numpy` AI | **Storage:** 5.8G free | **Build:** `app-debug.apk` 162M 23:45

**Method:** Traced `lib/core/routing/app_router.dart`, `backend/app/main.py`, `models/*.py`, `api/routes/*.py`, `services/*.py`, `l10n/*.arb`, `seed.py`, `tests/`, `docs/*`, `git status`

## Summary Counts

- COMPLETE: 28
- PARTIAL: 18
- MISSING: 8
- BROKEN: 1 (widget OTP label, not blocking)
- DUPLICATED: 0 (mock/api dual intentional)
- UNNECESSARY/BLOAT: 0 (rejected per FEATURE_DECISIONS)

## Detailed Classification

| # | Feature | Status | Evidence | Notes |
|---|---|---|---|---|
| 1 | **Auth OTP + JWT + RBAC** | COMPLETE | `api/routes/auth.py` `POST /send-otp` `verify-otp` `123456`, `core/security.py` HS256 30m, `deps.get_current_user` role `FARMER/CENTRE_OPERATOR/ADMIN`, `LocalStorage` token, `test_auth.py:3` passed | RBAC enforced per route |
| 2 | **Farmer Register/Profile** | COMPLETE | `farmers.py` `POST /farmers` `GET /farmers/me` `PATCH /me`, `lib/features/auth/*` `lib/features/farmer/profile_screen.dart` | Village/district/language |
| 3 | **Centre Discovery** | COMPLETE | `GET /centres` 4 TNCSC DPCs Erode, `centres_screen.dart` cards `status/commit queue/wait/slots/commodities`, `GET /centres/{id}/status` | No paid maps, lat/lng only |
| 4 | **Multi-commodity booking** | **MISSING** | `Booking.commodity_name String(50)` single, `slot_booking_screen.dart` single dropdown `AppConstants.commodities`, `bookings.py` `commodity` single | Spec requires list `Paddy 350kg + Maize 150kg` — need JSON `commodities` |
| 5 | **Single-commodity booking flow** | COMPLETE | `GET /slots?centre_id&date` 8/day 20cap, `GET /centres/{id}/slot-recommendations` 10-rule + `ai/slot-recommendation-ai`, `POST /bookings` atomic `with_for_update`, token `P{ordinal}` QR | Duplicate `400`, full `409`, past `400` |
| 6 | **Reschedule** | COMPLETE | `POST /bookings/{id}/reschedule {new_slot_id}` `bookings.py:158-206` atomic free old + reserve new + `calculate_wait` + notification `slot_confirmed` + audit `booking_rescheduled`, Flutter `booking_history_screen.dart` modal next-day slots | Valid past/full/mismatch checks |
| 7 | **Cancellation** | PARTIAL | `POST /bookings/{id}/cancel` `bookings.py:211` frees slot `AVAILABLE`, `CANCELLED`, `QueueEvent`, `booking_cancelled` notification, history shows | **Partial:** not blocked after irreversible `WEIGHMENT` — should prevent after `PROCUREMENT` stage |
| 8 | **Booking History** | COMPLETE | `GET /bookings` list sorted desc, `lib/features/bookings/presentation/booking_history_screen.dart` Upcoming/Active/Completed/Cancelled via `queueStatus`, detail sheet, reschedule/cancel actions | Was MISSING before 2026-09-18, now added |
| 9 | **My Procurement Day / Day Planner** | PARTIAL | `lib/features/planner/presentation/day_planner_screen.dart` `/planner` shows centre/date/commodity/qty/slot/token/ahead/wait/ETA/docs/timeline/reminder `Slot → Show Token`, but **missing** payment history, receipt, next action, centre status `BUSY/EMERGENCY`, weighment actual qty | Needs merge with procurement/payment to be single most useful screen |
| 10 | **Document Checklist** | PARTIAL | `day_planner_screen.dart:87 _docs=["Farmer ID","Aadhaar","passbook","sample"]` static, FAQ `assistant_service` `documents` | **Partial:** not configurable per centre/commodity, no `Required/Completed/Missing` per farmer (should be checklist state) |
| 11 | **Centre Information** | COMPLETE | `centres.py` `GET /centres/{id}/status` `is_open, queue, wait, available_slots`, `centres_screen.dart` shows name/location/hours`09-17`/commodities chips/counters/status/slots, `PATCH /centres/{id}` for operators | No external maps, OK |
| 12 | **Smart Reminders (notifications)** | PARTIAL | `notification_service` 10 types: `slot_confirmed, slot_reminder?`, `turn_approaching` `ahead<=2`, `token_called`, `procurement_stage`, `payment_status`, `booking_cancelled/rescheduled`, `centre closure`?; `queue.py:118` turn approaching once; `FCM` mock `[FCM-MOCK]` | **Partial:** `upcoming slot` 30m reminder not cron, `queue movement` not, `centre closure` not notifying affected |
| 13 | **No-show Management** | **MISSING** | `QueueStatus.NO_SHOW` enum exists `queue.py:15` + `allowed_transitions CALLED→NO_SHOW`, but **no operator UI** `operator_queue_screen.dart` menu shows `NO_SHOW` but not tested, no `release slot` logic, no audit `no_show` distinct? | Need dedicated mark NO_SHOW + audit + slot release + notify |
| 14 | **Operator Dashboard** | COMPLETE | `operator_dashboard_screen.dart` `GET /centres/{id}/dashboard` `todayFarmers/waiting/processing/completed/paymentPending/avgWait` + grid, `Peak Period` bar, `VIEW QUEUE` `ANALYTICS`, `Centre Controls` counters/capacity/status | Adequate |
| 15 | **Counter Management** | PARTIAL | `ProcurementCentre.active_counters int 0-10` `centres.py:83` `0-10` check, `scheduling_service` `load_score=qty_factor/counters fallback 1`, operator `_CentreControlsCard` dropdowns 0-5, capacity 10-30, status OPEN/BUSY/CLOSED | **Partial:** no `Counter 1 Active/Maintenance` per-counter, no workload per counter, scheduler uses aggregate not per-counter |
| 16 | **Weighment** | PARTIAL | `models/procurement.py:29 Weighment {procurement_id, gross, net, weighment_time, operator_id}` + `ProcurementService` advance `WEIGHMENT` creates record, but **no Flutter UI** for operator to enter `actual quantity/unit/notes` — only timeline `Advance Stage` button | Need form with validation `>0 <500 quintal` etc |
| 17 | **Quality Assessment** | PARTIAL | `models/procurement.py:39 QualityCheck {grade, moisture, remarks, checked_at}` similar, `advance` `QUALITY_CHECK`, but **no Flutter UI** for grade/moisture/remarks | Fields demo-oriented, not gov standard, needs form |
| 18 | **Procurement Timeline** | COMPLETE | `GET /procurements/{id}/timeline` 6 stages `BOOKING_CONFIRMED→ARRIVED→WEIGHMENT→QUALITY_CHECK→PROCUREMENT→COMPLETED` + `payment` 2 extra (8 total), `STAGE_TITLES`, `isCompleted/isCurrent/timestamp`, `POST /procurements/{id}/advance` validated via `procurement_service` order | No backward, operator only |
| 19 | **Payment Transparency** | COMPLETE | `GET /payments/{bid}` `commodity/qty/rate/total/status/transaction_id/payment_date`, `payment_screen.dart` timeline 4 steps `Booking→Procurement→Processing→Completed`, `POST /payments/{bid}/status` OPERATOR | History via same `GET /bookings` list, no gateway (spec) |
| 20 | **Digital Receipt** | **MISSING** | No receipt entity/API/Flutter screen; payment shows `transaction_id` but not printable `Farmer/Centre/Date/Commodity/Qty/Weighment/Procurement/Payment/Reference` | Need lightweight screen + `GET /procurements/{bid}/receipt` JSON, no PDF dep |
| 21 | **Centre Capacity** | PARTIAL | `Slot.capacity` 20 `booked` `available`, `centres.py:40 available_slots today`, `PATCH` updates today's `slot.capacity`, `scheduling` `availability_score`, but **no warning** `Centre approaching capacity` `occupancy>80%` | Need computed `USED/REMAINING/queue` + alert |
| 22 | **Temporary Closure / Emergency** | PARTIAL | `ProcurementCentre.status OPEN/BUSY/CLOSED` `is_active`, `PATCH` `status Closed→is_active false`, `GET /centres` filters `is_active`, `bookings` rejects `CENTRE_CLOSED`, but **no EMERGENCY mode**, no `reason` field, no `alternative centre/slot suggestion`, not notifying existing bookings | Need `EMERGENCY` + reason + affected list |
| 23 | **Congestion Alerts** | **MISSING** | No `GET /centres/{id}/alerts` rule-based `queue>threshold 15, wait>30, capacity>80%, counters<2, growth >5 in 10m` | Should be computed from `queue_tokens`, `slots`, `centre.active_counters` |
| 24 | **Smart Scheduling** | COMPLETE | `scheduling_service.py:39-134` 10 rules `past/full/hours/centre closed/safety buffer` + scoring `availability*congestion*load*time*buffer` `tie start_time,id` `get_recommendation` `wait=booked*avg/counters` `calculate_wait` ceil, `counters=0 fallback 1`, deterministic, `bookings.py` `with_for_update` + duplicate check | AI never bypasses, fallback |
| 25 | **AI Waiting-time prediction** | COMPLETE | `waiting_time_service.py` LR `y=b0+farmers+avg+qty+load+hour` `numpy lstsq` 800 synthetic `R2 0.79-0.85` `fallback calculate_wait`, `GET /ai/predict-wait`, `GET /ai/slot-recommendation-ai` `ai_used` + `rule_wait`, Flutter `ApiAiRepository` | Label `predicted 6 min (rule 5 min)` ✓ |
| 26 | **AI Centre Load** | COMPLETE | `centre_load_service.py` 7-day bookings linear trend `m*idx+c+occupancy*5+queue*0.1` `LOW <18 NORMAL <28 HIGH` 3d forecast, `GET /ai/centre-load/{id}` `level/score/reason/forecast_3d`, `analytics_screen` bar chart | Fallback occupancy |
| 27 | **AI Demand Forecasting (per commodity/date)** | **MISSING** | Only centre load 3d aggregate, **no** `GET /ai/demand-forecast?centre_id&commodity&date=` per-commodity | Need lightweight aggregate `bookings` by commodity/date trend + fallback baseline |
| 28 | **AI Anomaly Detection** | **MISSING** | No `GET /ai/anomalies` for `queue growth, processing delay, booking spike, capacity` | Need rule-based `bookings today vs avg 7d >50%`, `wait>30` etc, lightweight |
| 29 | **AI Explainability** | COMPLETE | `waiting_time_service reason "AI predicts 6 min (rule 5) based on 5 ahead, 3 counters, qty 10q"` + large qty suffix, `centre_load reason "LOW load — approx X bookings expected (trend slope m)"`, `slot reason "Lower expected centre load"` | Human-readable never unexplained |
| 30 | **Farmer Assistant** | COMPLETE | `assistant_service.py` FAQ en/ta/hi + DB context `token/slot/ahead/wait/payment/proc_stage`, `POST /assistant/ask` `language_code`, Flutter `assistant_screen.dart` chips `Where is my token?` | Never hallucinates |
| 31 | **Grievance/Feedback** | COMPLETE | `feedback.py` `POST /feedback {category,description}` `delay/quality/payment/other`, `GET /feedback` own/operator all, `POST /feedback/{id}/status` `OPEN/REVIEWED/RESOLVED`, Flutter `feedback_screen.dart` | Wired `main.py:50` |
| 32 | **Admin Dashboard** | COMPLETE | `GET /analytics/management` centres `total/completed/counters`, `demand_trend_7d`, `peak_periods`, `GET /analytics/operator/{id}?days=7` daily, `GET /analytics/farmer/{id}`, Flutter `/analytics` KPI `Farmers/day 342`, bar chart, utilization, CSV export button `GET /analytics/management` → `date,bookings` | Filtering by centre/date/commodity via query params exists `days` but not commodity |
| 33 | **Reports** | PARTIAL | CSV export button exists `analytics_screen.dart:68` snackbar `date,bookings`, but **no** dedicated `Daily centre/Procurement summary/Queue/Waiting/Payment/Commodity` reports screens | Need `GET /analytics/reports?type=` + Flutter list |
| 34 | **Audit Logging** | COMPLETE | `audit_logs` `actor/action/entity/details redacted 1000`, `audit_service.py` `booking_created/cancelled/rescheduled/token_called/arrived/processing/procurement/payment/centre_updated` 9 actions, `queue_service` `QueueEvent`, `procurement_events`, never logs `password/jwt` | Covers 9/11 required, missing `weighment/quality/counter change` distinct |
| 35 | **Offline / Low-network** | PARTIAL | `LocalStorage` `cached_booking_json/time, cached_queue_json/time` helpers, `DemoConfig.useMockBackend` fallback, `ErrorState Retry`, `LIVE` badge `farmer_home_screen:48`, but **no queued writes** for safe retry, not showing `Last updated` prominently in queue/token, `ApiClient` throws `HttpException` | Need `Last updated` banner + retry queue for feedback? |
| 36 | **Localization** | COMPLETE | `l10n/app_en.arb` 104, `ta 96`, `hi 96` keys buttons/errors/status/forms/empty/dialogs/assistant `Where is my token?`, `farmer_home_screen` uses hardcoded English `Good morning` greeting not via `AppLocalizations` | 90% localizable, greeting hardcoded |
| 37 | **Accessibility** | PARTIAL | `StatusChip` text+color `WAITING/FULL/AVAILABLE` `queue_screen`, touch 48dp, contrast `AppTheme` primaryGreen, but not full `Semantics` audit | Adequate |
| 38 | **Security** | COMPLETE | JWT HS256 `core/security.py`, RBAC `require_roles`, `bookings.py:152` farmer only own, `farmers.py:24` mobile must match, `centre patch` 403 farmer, input validation `FeedbackCreate` 5 chars, `SlotCreate` etc, `psycopg` parameterized, no FCM secrets committed `.env.example` empty, `audit_service` redacts |  |
| 39 | **DB Quality** | PARTIAL | 18 tables FK `onDelete CASCADE`, `UniqueConstraint centre_date_time`, `CheckConstraint booked<=capacity`, `indexes` `centre_id/date`, timestamps `created_at`, status fields, but **Alembic** `001_initial` stub `SELECT 1` still, no migration for `feedbacks/weighments/quality` — relies on `Base.metadata.create_all` in dev `main.py:64` | Need `alembic revision --autogenerate` for prod |
| 40 | **UI/UX Quality** | PARTIAL | Primary farmer flow simple `Home → Centres → Slots → Token → Queue → Planner` 5s, but **dead buttons?** `Payment receipt` missing, `History` reschedule only next-day (not date picker), `Offline` banner subtle, `Admin` filtering by commodity not, `Analytics` still mock KPI `342` hardcoded not live | Need UX pass to wire mock KPIs to live `manager` API |
| 41 | **Demo Data** | COMPLETE | `scripts/seed.py` 4 centres Erode 11.4/77.6 etc, 5 commodities MSP 2441/2461/4886/2400/8000, 8 slots/day×3 days 96 slots booked `[5,3,12,...]` includes nearly full 12/20, booking `P27 Bhavani 18.5q Paddy`, user `9876543210` farmer `Ravi Kumar Kavindapadi`, operator `9876543211`, `P1/P2` tokens, 4 queue tokens `WAITING/CALLED` etc | Realistic but synthetic, not `Test User 1` |

## Duplicated / Bloat Check

- No duplicated booking models (single `Booking` + `Slot` + `QueueToken` + `Procurement` + `Payment`), mock/api dual kept intentionally per `DemoConfig`
- Rejected bloat: blockchain/crypto, gamification, social feed, paid Maps, heavy BI/PDF, large LLM 7B, Firebase emulator suite, microservices — all not present

## Broken

- `test/phase2_widget_test.dart:52` `Expected "Enter OTP" found 0` — label mismatch `LoginScreen` shows `Verify OTP` not `Enter OTP`; `flutter test 14/15` pass, not blocking per `README:92`

